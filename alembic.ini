[alembic]
script_location = alembic
prepend_sys_path = .
path_separator = os

# The database URL is not set here: alembic/env.py reads DATABASE_URL via
# backend.db so migrations always target the same database the app uses.
